feat: migrate 6 more pages to PageHeader component

- passagem-turno, hh-semanal, nc, backlog, checklists, preditivas, programacao, equipe
- Alertas kept custom header (badge inline + timestamp — PageHeader doesn't support)
- tsc --noEmit + vite build both pass
